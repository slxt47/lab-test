# LAB ENV test

## Simple lab enviroment
```
local everything is reachible at servicename.localhost
```

```
if connected to a cloudflare tunnel u just need the same subdomain and it just works :)
```